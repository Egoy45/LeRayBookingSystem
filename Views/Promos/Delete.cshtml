@model LeRayBookingSystem.Models.Promos

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete Promo</h1>

<h3>Are you sure you want to delete this?</h3>
<hr />
<dl class="row">
    <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Title)</dt>
    <dd class="col-sm-10">@Html.DisplayFor(m => m.Title)</dd>

    <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Description)</dt>
    <dd class="col-sm-10">@Html.DisplayFor(m => m.Description)</dd>

    <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Price)</dt>
    <dd class="col-sm-10">@Html.DisplayFor(m => m.Price)</dd>

    <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Duration)</dt>
    <dd class="col-sm-10">@Html.DisplayFor(m => m.Duration)</dd>

    <dt class="col-sm-2">@Html.DisplayNameFor(m => m.PromoCode)</dt>
    <dd class="col-sm-10">@Html.DisplayFor(m => m.PromoCode)</dd>

    <dt class="col-sm-2">@Html.DisplayNameFor(m => m.StartDate)</dt>
    <dd class="col-sm-10">@Html.DisplayFor(m => m.StartDate)</dd>

    <dt class="col-sm-2">@Html.DisplayNameFor(m => m.EndDate)</dt>
    <dd class="col-sm-10">@Html.DisplayFor(m => m.EndDate)</dd>

    <dt class="col-sm-2">@Html.DisplayNameFor(m => m.IsActive)</dt>
    <dd class="col-sm-10">@Html.DisplayFor(m => m.IsActive)</dd>

    <dt class="col-sm-2">@Html.DisplayNameFor(m => m.CreatedByUser)</dt>
    <dd class="col-sm-10">@((Model.CreatedByUser?.FullName) ?? "N/A")</dd>

    <dt class="col-sm-2">@Html.DisplayNameFor(m => m.CreatedAt)</dt>
    <dd class="col-sm-10">@Html.DisplayFor(m => m.CreatedAt)</dd>
</dl>

<form asp-action="Delete">
    <input type="hidden" asp-for="Id" />
    <input type="submit" value="Delete" class="btn btn-danger" /> |
    <a asp-action="Index">Back to List</a>
</form>
